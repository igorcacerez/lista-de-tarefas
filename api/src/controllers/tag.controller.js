const listarTodas = require('./tag/listarTodas');
const inserirTag = require('./tag/inserirTag');
const alterarTag = require('./tag/alterarTag');
const deletarTag = require('./tag/deletarTag');

module.exports = {
    listarTodas,
    inserirTag,
    alterarTag,
    deletarTag
}